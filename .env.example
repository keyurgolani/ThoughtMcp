# ThoughtMCP Cognitive Architecture - Environment Configuration
# Copy this file to .env.development or .env.test and configure for your environment

# ============================================================================
# PostgreSQL Database Configuration (Required)
# ============================================================================
# Full connection string (alternative to individual settings below)
DATABASE_URL=postgresql://thoughtmcp_user:your_secure_password@localhost:5432/thoughtmcp

# Individual database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=thoughtmcp
DB_USER=thoughtmcp_user
DB_PASSWORD=your_secure_password

# Connection pool size (default: 20)
# Adjust based on expected concurrent operations
DB_POOL_SIZE=20

# ============================================================================
# Embedding Model Configuration (Required)
# ============================================================================
# Embedding model to use: ollama, e5, bge, openai
# - ollama: Local Ollama models (recommended for development)
# - e5: Sentence-transformers E5 models
# - bge: BAAI BGE models
# - openai: OpenAI API (requires API key)
EMBEDDING_MODEL=ollama

# Embedding vector dimension (must match model)
# - ollama (nomic-embed-text): 768
# - e5-large: 1024
# - bge-large: 1024
# - openai (text-embedding-3-small): 1536
EMBEDDING_DIMENSION=768

# Ollama host URL (only required if EMBEDDING_MODEL=ollama)
OLLAMA_HOST=http://localhost:11434

# OpenAI API key (only required if EMBEDDING_MODEL=openai)
# OPENAI_API_KEY=sk-your-api-key-here

# ============================================================================
# Logging Configuration
# ============================================================================
# Log level: DEBUG, INFO, WARN, ERROR
# - DEBUG: Verbose logging for development
# - INFO: General information
# - WARN: Warnings and important events (recommended for production)
# - ERROR: Only errors
LOG_LEVEL=DEBUG

# ============================================================================
# Environment
# ============================================================================
# Environment mode: development, production, test
NODE_ENV=development

# ============================================================================
# Performance Configuration
# ============================================================================
# Query cache TTL in seconds (default: 300 = 5 minutes)
CACHE_TTL=300

# Maximum processing time in milliseconds (default: 30000 = 30 seconds)
MAX_PROCESSING_TIME=30000

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================
# Enable query result caching (default: true)
# ENABLE_CACHE=true

# Enable performance monitoring (default: true)
# ENABLE_MONITORING=true

# Enable bias detection (default: true)
# ENABLE_BIAS_DETECTION=true

# Enable emotion detection (default: true)
# ENABLE_EMOTION_DETECTION=true
